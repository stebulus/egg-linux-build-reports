Build reports for https://github.com/stebulus/egg-linux .
These directories are produced by the `build-report` script there.

For an example of interpreting them, try
  ./flatten . |awk -f awk-example
to get a list of builds with one particular output.
